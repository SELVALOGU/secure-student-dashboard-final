/**
 * SecureEdu — Environment Configuration
 * This file centralizes the backend API URL for the static frontend.
 */
window.ENV = {
    // Replace this with your actual Render backend URL (e.g., 'https://secure-student-analyzer.onrender.com')
    // Or if hosting frontend on the exact SAME site as backend, use empty string ''
    API_BASE_URL: 'https://your-backend-app.onrender.com'
};
