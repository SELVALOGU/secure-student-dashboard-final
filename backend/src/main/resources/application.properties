server.port=8080
server.servlet.session.timeout=60m

# Fallback database connection properties. 
# In production, these should be overridden by environment variables DB_URL, DB_USER, DB_PASS
db.url=jdbc:postgresql://localhost:5432/securedb
db.user=postgres
db.password=root
